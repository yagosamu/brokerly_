# CRM Sprint 18 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the tenant-aware CRM app with pipelines, stages, deals, Kanban/grid views, AJAX movement, default seeding, and PRD completion updates.

**Architecture:** Add a focused `crm` Django app following the existing Brokerly patterns for models, forms, CBVs, URLs, admin, services, signals, and management commands. Keep UI templates/static/sidebar out of scope.

**Tech Stack:** Python 3.13+, Django 6, PostgreSQL, existing Brokerly tenant mixins and model conventions.

---

### Task 1: App scaffold and registration

**Files:**
- Create: `crm/`
- Modify: `core/settings.py`
- Modify: `core/urls.py`

- [ ] Run `docker compose exec app python manage.py startapp crm`.
- [ ] Remove `crm/tests.py`.
- [ ] Register `'crm'` after `'commissions'`.
- [ ] Include `path('crm/', include('crm.urls'))`.

### Task 2: Domain models and services

**Files:**
- Create: `crm/models.py`
- Create: `crm/services.py`
- Create: `crm/signals.py`
- Modify: `crm/apps.py`

- [ ] Implement `Pipeline`, `Stage`, `Deal`, and `DealStageHistory`.
- [ ] Add database constraints/indexes from the sprint brief.
- [ ] Add model `clean()`/`save()` tenant validation for related FKs.
- [ ] Implement idempotent default pipeline seeding.
- [ ] Implement stage movement with history and status synchronization.
- [ ] Register `post_save(Brokerage)` signal in `ready()`.

### Task 3: Forms, views, URLs, admin, command

**Files:**
- Create: `crm/forms.py`
- Create: `crm/views.py`
- Create: `crm/urls.py`
- Create: `crm/admin.py`
- Create: `crm/management/commands/backfill_pipelines.py`

- [ ] Implement pipeline/stage forms and inline formset.
- [ ] Implement deal form, search form, and move form.
- [ ] Implement pipeline CRUD views.
- [ ] Implement deal grid/detail/create/update views.
- [ ] Implement Kanban context and AJAX move endpoint.
- [ ] Register nine CRM URLs.
- [ ] Register all CRM models in admin.
- [ ] Add the backfill management command.

### Task 4: Migrations, verification, and PRD

**Files:**
- Create: `crm/migrations/0001_initial.py`
- Modify: `PRD.md`

- [ ] Run `makemigrations crm`.
- [ ] Run `migrate`.
- [ ] Run `backfill_pipelines`.
- [ ] Run `manage.py check`.
- [ ] Run the provided shell scenario.
- [ ] Mark Sprint 18 tasks as `[x]`.
- [ ] Commit documentation, implementation, and PRD completion atomically.
